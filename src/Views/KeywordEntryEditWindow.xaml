<Window x:Class="Flow.Launcher.Plugin.AliasFlow.Views.KeywordEntryEditWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Keyword Entry" Height="320" Width="540"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="110"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0" Grid.Column="0" Margin="0,0,8,8" VerticalAlignment="Center" Text="Title"/>
        <TextBox x:Name="TitleBox" Grid.Row="0" Grid.Column="1" Margin="0,0,0,8"/>

        <TextBlock Grid.Row="1" Grid.Column="0" Margin="0,0,8,8" VerticalAlignment="Center" Text="Path"/>
        <TextBox x:Name="PathBox" Grid.Row="1" Grid.Column="1" Margin="0,0,0,8"/>

        <TextBlock Grid.Row="2" Grid.Column="0" Margin="0,0,8,8" VerticalAlignment="Center" Text="Keywords"/>
        <TextBox x:Name="KeywordsBox" Grid.Row="2" Grid.Column="1" Margin="0,0,0,8"
                 ToolTip="쉼표(,)로 구분해서 입력"/>

        <TextBlock Grid.Row="3" Grid.Column="0" Margin="0,0,8,8" VerticalAlignment="Center" Text="Description"/>
        <TextBox x:Name="DescBox" Grid.Row="3" Grid.Column="1" Margin="0,0,0,8"/>

        <TextBlock x:Name="ErrorText" Grid.Row="4" Grid.ColumnSpan="2"
                   Foreground="Tomato" TextWrapping="Wrap" />

        <StackPanel Grid.Row="5" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Width="90" Margin="0,0,8,0" Content="Cancel" Click="Cancel_Click"/>
            <Button Width="90" Content="OK" Click="Ok_Click"/>
        </StackPanel>
    </Grid>
</Window>
